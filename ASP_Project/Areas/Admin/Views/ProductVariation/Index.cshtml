@* ﻿@using ASP_Project.Areas.Identity.Data.Models;
@model IEnumerable<ProductVariation>
@{
    ViewData["Title"] = "Product Variations";
    var productImages = ViewBag.ProductImages as List<ProductImage>;
}

<!DOCTYPE html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link rel="stylesheet" href="~/css/productIndex.css" />
</head>
<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Product Variation List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="ProductVariation" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> &nbsp; Create New Product Variation
            </a>
        </div>
    </div>
    <br /><br />
    <div class="card-deck container">
        <div class="row row-cols-auto">
            @foreach (var item in Model)
            {
                <div class="card col" style="width: 18rem;">
                    @if (item.ProductImages != null && item.ProductImages.Any())
                    {
                        <img class="card-img-top" alt="Product Image" src="data:image/jpeg;base64,@Convert.ToBase64String(item.ProductImages.First().ImageData)" />
                    }
                    else
                    {
                        <p>No Image Available</p>
                    } 
                    <div class="card-body">
                        <h5 class="card-title">@item.Product.Name</h5>
                        <p class="card-text">Price: @item.Price</p>
                        <p class="card-text">Internal memory: @item.InternalMemory</p>
                        <p class="card-text">RAM: @item.RAM</p>
                        <p class="card-text">Color: @item.Color</p>
                        <p class="card-text">Quantity: @item.Quantity</p>
                        <p class="card-text">Discount: @item.Discount</p>
                        <p class="card-text">Prosucer: @item.Product.Producer</p>
                        <div >
                            <a asp-controller="ProductVariation" asp-action="Edit" asp-route-id="@item.Id"
                               class="btn btn-primary mx-2"> <i class="bi bi-pencil-square"></i> Edit</a>
                            <a asp-controller="Product" asp-action="Delete" asp-route-id="@item.Id"
                               class="btn btn-danger mx-2"> <i class="bi bi-trash-fill"></i> Delete</a>
                        </div>
                    </div>
                </div> 
            }
        </div>
    </div>
    <table id="tblData" class="table table-bordered table-striped" style="width:100%">
        <thead>
            <tr>
                <th style="text-align: center;">Name</th>
                <th style="text-align: center;">Price</th>
                <th style="text-align: center;">Internal memory</th>
                <th style="text-align: center;">RAM</th>
                <th style="text-align: center;">Color</th>
                <th style="text-align: center;">Quantity</th>
                <th style="text-align: center;">Discount</th>
                <th style="text-align: center;">Product</th>
                <th style="text-align: center;">Images</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr> 
                    <td width="10%">
                        @item.Price
                    </td>
                    <td width="10%">
                        @item.InternalMemory
                    </td>
                    <td width="10%">
                        @item.RAM
                    </td>
                    <td width="10%">
                        @item.Color
                    </td>
                    <td width="10%">
                        @item.Quantity
                    </td>
                    <td width="10%">
                        @item.Discount
                    </td>
                    <td width="10%">
                        @item.Product.Producer
                    </td>
                    <td class="td td-img">
                        @if (item.ProductImages != null && item.ProductImages.Any())
                        {
                            <img class="img" alt="Product Image" src="data:image/jpeg;base64,@Convert.ToBase64String(item.ProductImages.First().ImageData)" />
                        }
                        else
                        {
                            <p>No Image Available</p>
                        }
                    </td>
                    <td width="15%">
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller="ProductVariation" asp-action="Edit" asp-route-id="@item.Id"
                               class="btn btn-primary mx-2"> <i class="bi bi-pencil-square"></i> Edit</a> 
                             <a asp-controller="Product" asp-action="Delete" asp-route-id="@item.Id" 
                                  class="btn btn-danger mx-2"> <i class="bi bi-trash-fill"></i> Delete</a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div> *@